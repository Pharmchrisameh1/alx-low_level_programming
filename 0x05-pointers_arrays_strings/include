#include "main.h"

/**
 * swap_int - swaps the value of two integers
 * @a: the first integer
 * @b: the second integer
 *
 */

void swap_int(int *a, int *b)
{
	int m, n;

	m = *a;
	n = *b;
	*a = n;
	*b = m;
}
